<?xml version="1.0" encoding="utf-8"?>
<!-- Layout cho 1 chỗ đỗ xe -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardChoDo"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <RelativeLayout
        android:id="@+id/layoutChoDo"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:background="@drawable/bg_cho_do_trong"
        android:padding="12dp">

        <!-- Số thứ tự chỗ đỗ -->
        <TextView
            android:id="@+id/txtSoChoDo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:background="@drawable/bg_gradient_gold"
            android:gravity="center"
            android:text="1"
            android:textColor="@color/den_sang_trong"
            android:textSize="20sp"
            android:textStyle="bold"
            android:elevation="2dp"/>

        <!-- Icon xe hoặc trống -->
        <ImageView
            android:id="@+id/imgTrangThaiChoDo"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_parking_empty"
            android:contentDescription="Trạng thái chỗ đỗ"/>

        <!-- Thông tin bên dưới -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="vertical"
            android:gravity="center">

            <!-- Biển số xe -->
            <TextView
                android:id="@+id/txtBienSoXe"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/trang_thai_trong"
                android:textColor="@color/trang_tinh_khiet"
                android:textSize="16sp"
                android:textStyle="bold"
                android:shadowColor="#80000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="2"/>

            <!-- Thời gian đỗ -->
            <TextView
                android:id="@+id/txtThoiGianDo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#E0E0E0"
                android:textSize="12sp"
                android:layout_marginTop="4dp"
                android:visibility="gone"/>

        </LinearLayout>

    </RelativeLayout>

</androidx.cardview.widget.CardView>